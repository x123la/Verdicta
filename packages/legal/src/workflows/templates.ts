import {
  caseBriefSchema,
  jurisprudenceComparisonSchema,
  memoOutlineSchema,
  studyModeSchema,
  supportReviewSchema
} from "@verdicta/shared";

export const workflowSchemas = {
  caseBrief: caseBriefSchema,
  jurisprudenceComparison: jurisprudenceComparisonSchema,
  memoOutline: memoOutlineSchema,
  studyMode: studyModeSchema,
  supportReview: supportReviewSchema
};
