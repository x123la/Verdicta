{
  "name": "@verdicta/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc -b",
    "lint": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@verdicta/ai": "workspace:*",
    "@verdicta/db": "workspace:*",
    "@verdicta/legal": "workspace:*",
    "@verdicta/shared": "workspace:*",
    "@verdicta/ingestion": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
